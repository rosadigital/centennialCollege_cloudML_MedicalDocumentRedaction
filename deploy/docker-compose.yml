services:
  api:
    image: ${ECR_REGISTRY:?set ECR_REGISTRY}/${ECR_REPOSITORY:?set ECR_REPOSITORY}:${IMAGE_TAG:-latest}
    container_name: medical-redaction-server
    restart: always
    env_file:
      - backend.env
    ports:
      - "${SERVER_PORT:-8000}:8000"
